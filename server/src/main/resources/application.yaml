spring:
  application:
    name: carboncalculator

  datasource:
    url: ${DATABASE_URL}
    username: ${APP_DATABASE_USERNAME:app}
    password: ${APP_DATABASE_PASSWORD:app}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    user: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}

app:
  jwt:
    secret: ${JWT_SECRET}

server:
  port: ${PORT:8080}
  servlet:
    context-path: /api/v1
